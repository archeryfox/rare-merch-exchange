# Переменные окружения для Render.com
# Эти данные нужно будет добавить в Web Service после его создания

# Сервер
SERVER_PORT=10000
SERVER_HOST=0.0.0.0
GIN_MODE=release

# База данных PostgreSQL (Render)
DB_HOST=dpg-d3rvapq4d50c73fnc320-a
DB_PORT=5432
DB_USER=rare_merch_exchange_db_user
DB_PASSWORD=YOUR_DB_PASSWORD_FROM_RENDER_DASHBOARD
DB_NAME=rare_merch_exchange_db
DB_SSL_MODE=require

# Redis (Render)
REDIS_HOST=red-d3rvarjuibrs739i7b3g
REDIS_PORT=6379
REDIS_PASSWORD=YOUR_REDIS_PASSWORD_FROM_RENDER_DASHBOARD
REDIS_DB=0

# JWT (ОБЯЗАТЕЛЬНО ИЗМЕНИТЕ В ПРОДАКШНЕ!)
JWT_SECRET=your-super-secret-jwt-key-change-in-production-render
JWT_EXPIRE_HOURS=24

# Загрузка файлов
UPLOAD_MAX_SIZE=52428800
UPLOAD_PATH=/app/uploads

# Аукционы
AUCTION_SOFT_CLOSE_MINUTES=5
AUCTION_MIN_INCREMENT_PERCENT=5

# Лотереи
LOTTERY_MAX_TICKETS_PER_USER=10
LOTTERY_MIN_TICKETS=1

# Комиссии
COMMISSION_RATE_VERIFIED=0.05
COMMISSION_RATE_REGULAR=0.08

# Эскроу
ESCROW_AUTO_RELEASE_DAYS=7
ESCROW_DISPUTE_PERIOD_DAYS=3

# Антифрод
ANTIFRAUD_MAX_BIDS_PER_MINUTE=10
ANTIFRAUD_SUSPICIOUS_ACTIVITY_THRESHOLD=5

# Логирование
LOG_LEVEL=warn
LOG_FORMAT=json
